services:
  - type: web
    runtime: node
    name: app
    buildCommand: npm install && npm run build && npm run build:cli
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
